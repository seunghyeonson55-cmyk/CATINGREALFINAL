# CATING 웹 버전(정식 디자인) — 작업 지시서

> 이 문서는 **새 대화(새 세션)** 에서 "시안 디자인대로 정식 웹 버전을 만들어줘" 할 때 읽는 지시서입니다.
> 목표: 현재 Streamlit 프로토타입의 **모든 기능을 유지**하되, **시안 디자인(다크 헤더 + 드롭다운 + 깔끔한 카드)** 으로 픽셀 수준 자유도를 가진 진짜 웹앱으로 재구성.
> 사용자는 비개발자입니다 — 모든 코드·실행·배포를 Claude가 대신하고, 쉬운 한국어로 단계별 설명하세요.

---

## 0. 한 줄 요약
감독이 **자연어 문장 또는 얼굴 사진**으로 원하는 배우의 "이미지/분위기"를 검색하면, 등록 배우 중 **가장 잘 맞는 사람을 매칭도 순**으로 찾아주는 캐스팅 도구. 공고 등록·지원·결과발표·메시지까지.

## 1. 추천 아키텍처
- **프론트엔드: Next.js(React) + TailwindCSS** — 시안 100% 구현(다크 헤더, 호버 드롭다운, 카드).
- **백엔드: FastAPI(파이썬)** — **기존 엔진을 그대로 재활용**해 REST API로 노출.
- **DB: Supabase(Postgres)** — 이미 쓰는 것 그대로(`feedback_db.py`의 스키마/연결).
- 배포: 프론트=Vercel, 백엔드=Render/Railway(또는 둘 다 한 곳). 얼굴 모델은 백엔드에서 1회 다운로드 캐시.
- **폴더는 현재 앱(`cating/`)과 분리**해서 새로(예: `cating-web/`). 같은 파일 동시 편집 금지.

## 2. 그대로 재활용할 '엔진'(중요 — 새로 안 만든다)
현재 `cating/` 폴더의 검증된 파이썬 로직을 FastAPI로 감싸기만 하면 됨:
- **`engine.py`** — 임베딩(텍스트 `text-embedding-3-large`) + 코사인 검색(`search_filtered`, `passes_filters`, `get_embedder`).
- **`facevec.py`** — **얼굴 시각 벡터(OpenCV YuNet+SFace, 무료/로컬)**. `face_vector(img_bytes)→128d`, `cosine`. 같은 사람 ~1.0. 모델은 OpenCV Zoo에서 1회 다운로드.
- **`feedback_db.py`** — Supabase/SQLite 이중 백엔드. 공고/지원/알림/메시지/유저/세션/찜/오디션/댓글 전부. psycopg3, `_Conn` 래퍼.
- **`intake.py`** — 파일(PDF/이미지/DOCX/ZIP)→얼굴 크롭·나이/키 파싱.
- **`app.py` 안의 핵심 함수**(분석 프롬프트·검색 흐름)는 백엔드 서비스로 옮긴다:
  - `analyze_person_faces(face_pngs, detailed=True)` — 사진→인상 분석(JSON: desc/keywords/traits/gender/age). **프롬프트는 app.py 그대로 가져올 것**(원초적 얼굴 기준·분위기는 얼굴에서 나옴).
  - `expand_query(q)` — 검색어→인상 묘사(레퍼런스 배우명도 변환). 배우 분석과 같은 형식.
  - 임베딩 텍스트 = `desc + " / " + keywords + " / 강한 인상: ..."` (대칭 매칭).

## 3. 검색 로직(반드시 이대로)
- **텍스트 검색** = 분위기. `expand_query`→임베딩→배우 임베딩과 코사인. **얼굴(원초적)에서 나온 분위기** 기준.
- **이미지 검색** = 얼굴 자체. `facevec.face_vector`로 올린 얼굴 ↔ 등록 배우 얼굴 직접 대조.
  - **사진만**: 얼굴 100%. **사진+텍스트**: 얼굴 90% + 분위기 10%(`FACE_WEIGHT=0.9`).
  - 표시 매칭도는 SFace 코사인을 기준선(0.363) 보정해 환산(`_face_pct`). 음수→0.
  - 배우 얼굴 벡터는 등록 시 1회 계산해 저장(검색 때 재분석 X)하는 게 이상적.
- **정량 필터**(성별/나이 0~100/키 140~210/목소리)는 하드 필터.
- 초상권: **동의한 등록 배우 + 권리 있는 레퍼런스 사진**에만 얼굴 벡터 사용. 연예인 얼굴 DB 금지.

## 4. 화면/기능 목록(전부 포팅)
**공통**
- 다크 상단 헤더: 로고(CATING, 세리프) + 메뉴 + 우측 [내 메시지][내 프로필][로그아웃(빨강)]. **현재 페이지 메뉴 하이라이트.**
- 감독 메뉴: **배우 탐색** / **배우 모집하기**(드롭다운: 공고 등록·올린 공고·배우지원서 업로드).
- 배우 메뉴: 공고 보기 / 내 지원 / 알림 / 메시지 / 내 프로필.
- 로그인: 이메일 인증 회원가입 + 비밀번호 + 비번 재설정 + 30일 쿠키 로그인 유지(`sessions` 테이블).
- 역할: 가입 시 감독/배우 선택. 배우는 **프로필(사진 필수) 등록해야 가입 완료**.

**배우 탐색(시안 메인 화면)**
- 둥근 검색 박스(문장 입력 + 「이미지 첨부」 + 첨부 미리보기/제거) → 정량 필터 박스 → 큰 **「탐색하기」** 버튼.
- 추천 표현 칩(무료, 클릭하면 검색어에 추가) + 선택적 AI 추천.
- 결과 카드: 얼굴·매칭도%·인상 묘사·키워드·(감독)찜/👍/👎/상세/메시지. 전체순위 토글.
- 배우 본인은 「내 프로필」에서 **배우 탐색 공개 on/off**(listed) 언제든.

**공고(감독)**
- 공고 등록: 제목·작품 분류·작품명*·**모집 기간(시작~끝, 시작 미래면 예약공개)**·시놉시스*·페이(단위 선택+금액)*·장르·참고사진·배역(이름/설명/이미지/성별/나이범위)·필수 제출항목·동영상 필수.
- 올린 공고: 목록(작품 분류·상태 필터, 클릭형 카드) → 공고(지원링크 맨 위·배역별 지원자·오디션·댓글·마감/삭제) → 배역(지원자 평가).
- 지원자 평가: **별점(1~5)** → 🎬오디션 대상 → ✅최종 결정(합격/불합격/미정). 합격/불합격은 **비공개 표시**(배우엔 '검토중'), **결과 발표** 때 일괄 통보. **합격자별 맞춤 메시지** 작성 후 전송.
- 결과: 미정→발표 시 불합격. 알림 클릭하면 그 공고 다시 보기. 삭제 시 '검토중'에게만 알림.

**지원(배우/비회원→로그인)**
- `?apply=<랜덤토큰>` 링크 → 공고 카드 + 배역 선택(선택 배역 **모집 조건: 성별·나이** 표시) → 프로필 기반 자동지원 or 폼. 로그인 필수.
- 내 지원: 상태(발표 전 '검토중'), 지원한 공고 다시 보기.

**메시지**
- 감독↔배우 1:1. 대화별 안읽음 배지, **읽음 표시**, 3초 자동갱신, 맨아래 자동 스크롤, 입력칸 하단 고정.

**알림 / 찜 / 오디션 / 공고 댓글 / 계정 탈퇴 / 관리자(`?admin`)** 도 포팅.

## 5. DB 스키마(이미 있음 — 그대로)
`feedback_db.py` 참고. 주요 테이블: `users, sessions, applicants, casting_calls, applications(rating·audition·seen·status), notifications, messages(read), shortlists, audition_slots/picks, call_comments`. 컬럼 추가 시 `_alter_add` + 운영 DB 직접 ALTER. 비밀은 `.streamlit/secrets.toml`/환경변수(공개 저장소 보호).

## 6. 디자인 방향(시안)
- 헤더: 짙은 차콜(#2b2b2b~#1f1f1f) 그라데이션, 로고 세리프(Fraunces), 메뉴 호버/액티브 하이라이트, 로그아웃은 진한 빨강.
- 본문: 밝은 배경(#FBFAF7), 둥근 카드(radius 18~22), 부드러운 그림자. 본문 폰트 Pretendard.
- 배우 탐색: 첨부한 시안 이미지(검색박스+필터박스+탐색하기) 그대로.

## 7. 작업 순서(점진적)
1. FastAPI 뼈대 + 엔진 import + `/search`(텍스트/이미지) `/auth` 엔드포인트.
2. Next.js 헤더 + 배우 탐색 화면(시안).
3. 공고/지원 → 메시지/알림 → 나머지.
각 단계마다 사용자가 테스트하도록 배포.

## 8. 주의(현재 프로젝트에서 배운 것)
- Streamlit 핫리로드 KeyError는 그쪽엔 없음(다른 스택). 무시.
- 얼굴 분석/검색 철학은 `cating_search_philosophy` 메모 + 본 문서 3장 준수.
- 비용: 분석은 배우 1명당 1회(등록 시), 검색은 텍스트 비교(저렴), 얼굴 벡터 무료.
